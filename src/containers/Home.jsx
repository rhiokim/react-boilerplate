import React from 'react';

export default class Home extends React.Component {
  render() {
    return (
      <div className="padded-more">
        <h1>Welcome to React boilerplate with Photon</h1>
        <p>
          Thanks for downloading <a href="http://photonkit.com/" target="_blank">Photon</a>. This is an example HTML page that's linked up to compiled Photon CSS, has the proper meta tags
          and the HTML structure.
        </p>
      </div>
    );
  }
}
